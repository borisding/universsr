const root = require('app-root-path');

module.exports = {
  root,
  assets: `${root}/assets`,
  config: `${root}/config`,
  logs: `${root}/logs`,
  public: `${root}/public`,
  src: `${root}/src`,
  webpack: `${root}/webpack`
};
