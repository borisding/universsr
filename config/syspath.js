const root = require('app-root-path');

module.exports = {
  root,
  api: `${root}/api`,
  app: `${root}/app`,
  assets: `${root}/assets`,
  config: `${root}/config`,
  logger: `${root}/logger`,
  middleware: `${root}/middleware`,
  public: `${root}/public`,
  storage: `${root}/storage`,
  utils: `${root}/utils`
};
