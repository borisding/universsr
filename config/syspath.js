const root = require('app-root-path');

module.exports = {
  root,
  api: `${root}/api`,
  app: `${root}/app`,
  assets: `${root}/assets`,
  config: `${root}/config`,
  logger: `${root}/logger`,
  logs: `${root}/logs`,
  middleware: `${root}/middleware`,
  public: `${root}/public`,
  utils: `${root}/utils`,
  webpack: `${root}/webpack`
};
