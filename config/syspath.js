const ROOT = require('path').resolve(process.cwd());

module.exports = {
  ROOT,
  BIN: `${ROOT}/bin`,
  RESOURCES: `${ROOT}/resources`,
  CONFIG: `${ROOT}/config`,
  LOGS: `${ROOT}/logs`,
  PUBLIC: `${ROOT}/public`,
  SRC: `${ROOT}/src`
};
