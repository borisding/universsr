import root from 'app-root-path';

export default {
  root,
  app: `${root}/app`,
  assets: `${root}/assets`,
  build: `${root}/build`,
  config: `${root}/config`,
  public: `${root}/public`,
  storage: `${root}/storage`,
  webpack: `${root}/webpack`
};
